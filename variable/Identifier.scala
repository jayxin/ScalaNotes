object Identifier {
  def main(args : Array[String]) : Unit = {
    val ++ = "hello, world"
    println(++)

    val -+*/ = 90
    println(-+*/)

    // ++ => $plus$plus

    //val +q = "abc" // Error

    var `true` = "hello, scala!"
    println(`true`)

    var Int = 90.45
    println(Int)

    //var _ = "jack"
    //println(_) // error
  }
}
